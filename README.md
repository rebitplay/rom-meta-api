# Rebit Rom Metadata API

This repository contains the API for looking up metadata for ROM files of Rebit online games. The API allows users to query information about various ROMs, including their titles, descriptions, release dates, and other relevant metadata.